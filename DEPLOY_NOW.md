# Deploy now

The ZIP root contains `index.html`; there is no enclosing folder. This layout is suitable for a static-host drag-and-drop deployment or for extracting into a GitHub repository root.

A live public URL still requires an authenticated hosting account.
